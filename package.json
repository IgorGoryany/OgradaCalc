{
  "name": "ogradacalc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "predeploy": "parcel build index.html --out-dir=public",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ksur",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "vercel": "^29.1.1"
  }
}
